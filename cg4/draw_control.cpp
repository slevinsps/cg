#include "structs.h"
#include "QColor"
#include <vector>
#include <QGraphicsScene>
#include "draw_circle.h"
#include "mainwindow.h"
#include "glwidget.h"

/*void draw_control(std::vector<options_s> opt)
{
    int paint = 1;
    for (int i = 0; i < opt.size(); i++)
    {
        if (opt[i].choose == 1)
        {

            if (opt[i].method == 1)
            {
                //draw_canonich_circle(fPainter, opt[i], paint);
            }
            if (opt[i].method == 3)
            {
                //draw_brez_ellipse(opt[i], paint);
            }
            if (opt[i].method == 4)
            {
                //draw_midle_ellipse(opt[i], paint);
            }
        }
    }
}*/

